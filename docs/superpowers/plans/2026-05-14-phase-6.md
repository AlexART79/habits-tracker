# Phase 6 Final Acceptance, README Cleanup, and Hardening Plan

## Summary

Bring the project to final evaluation shape by replacing README history/status content with a clean user-facing setup guide, moving acceptance evidence into docs, and running the full required verification gate.

Use the existing Phase 6 scope from `docs/phased-development-plan.md`, but apply the user override: README must not contain development history, phase status, implementation plans, or acceptance checklist content.

## Key Changes

- Rewrite `README.md` as a concise project entry point containing only:
  - Project name: Habit Tracker with Streaks
  - User-facing description of what the app does
  - High-level project structure
  - Tech stack
  - Prerequisites, install, env setup, database setup, local run commands, seed command, test/typecheck/lint commands
  - OAuth setup needed to run Google/GitHub sign-in locally
  - Brief Docker note as setup reality: Docker is not configured/required; local npm workflow is supported
- Remove README sections that read like development history or phase tracking:
  - `Project Status`
  - `Implemented now`
  - phase references such as “Phase 0” / “Phase 5”
  - acceptance checklist
  - long implementation-rule explanations
- Create `docs/acceptance.md` for evaluator-facing verification:
  - Phase 6 checklist from the plan
  - verification command results area
  - manual acceptance notes for Google/GitHub OAuth, WebSocket milestones, and local runtime smoke checks
- Keep detailed behavior facts out of README unless needed to run the app. If detailed API/WebSocket/streak behavior must remain documented, place it in `docs/acceptance.md` or a short `docs/runtime-reference.md`, with README linking to it only from a compact “Additional docs” line.

## Implementation Steps

1. Inspect current README and confirm each command against `package.json` and `apps/api/.env.example`.
2. Replace README content with the clean structure:
   - Overview
   - Project structure
   - Tech stack
   - Prerequisites
   - Install
   - Environment
   - Database
   - Run locally
   - Verification commands
   - OAuth setup
   - Docker note
3. Add `docs/acceptance.md` with the final checklist:
   - sign-in with Google/GitHub
   - first SSO creates user
   - habit CRUD
   - today check-in/undo
   - streak metrics
   - search/filter
   - cross-user blocking
   - 3/7/30 WebSocket milestones
   - milestone non-repeat after ack/reconnect
   - client sends `milestones.subscribe`
   - app runs locally
   - tests pass locally
4. Hardening pass:
   - Run static verification first: `npm run typecheck`, `npm run lint`, `npm test`
   - If failures appear, fix root causes with focused automated tests.
   - Re-run all three root commands after fixes.
5. Runtime acceptance pass:
   - Start app with `npm run dev`.
   - Confirm API health at `http://localhost:3001/api/health`.
   - Confirm web app at `http://localhost:5174`.
   - Use documented seed/manual flow to verify at least one milestone notification path where possible.
   - Record results in `docs/acceptance.md`.

## Test Plan

- Required final gate:
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
- README command accuracy:
  - Compare documented commands against root scripts and API env example.
  - Ensure all commands are PowerShell-compatible.
- Manual acceptance:
  - Local app starts from README commands.
  - OAuth setup instructions match callback URLs.
  - WebSocket notification manual verification path is documented outside README.

## Assumptions

- README cleanup prioritizes the user’s “no development history and plan” instruction over the older Phase 6 README checklist wording.
- Acceptance checklist belongs in `docs/acceptance.md`, not README.
- No new frameworks or broad refactors are needed unless verification exposes a concrete bug.
